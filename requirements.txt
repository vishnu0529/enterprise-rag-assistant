# Pinned to versions verified working together in this project (see README
# for why: newer sentence-transformers/torch combos have ABI/version
# conflicts on some platforms — these exact pins are known-good).

fastapi==0.141.1
uvicorn[standard]==0.52.0
pydantic==2.13.4
pydantic-settings==2.14.2

# LLM providers
google-genai==2.15.0
anthropic==0.120.2

# RAG pipeline
langchain==1.3.14
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langgraph==1.2.11
qdrant-client==1.18.0

# Embeddings — fastembed (ONNX Runtime), not sentence-transformers+torch.
# torch's baseline memory footprint alone OOM'd this app on Render's free
# tier (512MB); fastembed runs the same class of small models without it.
fastembed>=0.4.0
numpy  # evaluation.py's cosine-similarity math; no longer capped <2, that
       # pin existed only for torch/sentence-transformers ABI compatibility

# Document parsing
pdfplumber==0.11.10

# Session storage
sqlmodel==0.0.39
psycopg2-binary==2.9.12

# Evaluation
# NOTE: ragas==0.4.3 has a broken import in this environment
# (langchain_community.chat_models.vertexai does not exist in the
# langchain-community version above). RAGAS-methodology metrics
# (faithfulness, answer relevancy, context precision/recall) are
# implemented directly in app/services/evaluation.py instead — no
# ragas/datasets dependency needed (numpy, pinned above, covers its
# cosine-similarity math).

# Demo UI
streamlit==1.60.0

# Utilities
python-dotenv==1.2.2
requests==2.34.2
tiktoken==0.13.0

# Dev / test / lint
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
ruff==0.16.0
